#include <stdio.h>
#include <string.h>
int main()
{
    char a[100] = "apple";
    char b[100] = "orange";

    // LOGIC FOR COPYING STRING;
    // for (int i = 0; i <= strlen(b); i++)
    // {
    //     a[i] = b[i];
    // }

    // strcpy(a, b);

    printf("%s", a);

    return 0;
}